#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

  /*
  int num_arrays = 5;
  int size = 5;

  int * p = ( int * ) calloc( num_arrays, sizeof( int ) );

  
  while ( *p ) {
    *p = 3;
    printf ( "%d\n", *p );
    p++;
  }
  


  return 0;
  */

  printf ( "No working :(\n" );
  return 0;
}
